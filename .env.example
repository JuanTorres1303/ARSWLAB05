# Déjala vacía para usar el proxy de Vite (ver vite.config.js) contra el
# backend real en http://localhost:8080 sin problemas de CORS.
# Si tu backend sí tiene CORS habilitado, puedes apuntar aquí directamente,
# p. ej. VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=
VITE_USE_MOCK=true
